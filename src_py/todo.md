
1. Finish user CMake build and integrate it into startup of C++ classifier
2. Test that we can link against a user's static lib and utilize their functions

# Post-classification creation
Maybe return data from C++ extract functions to then be returned to python
Beautify object definitions instead of just functions 
Codeblock like definition trees? 